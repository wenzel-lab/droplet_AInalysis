IMGSZ = 1024 # size of the image that is analysed
CONFIDENCE = 0.85 # minimum confidence that a prediction needs to be counted as a droplet
PIXEL_RATIO = 1 # must be an integer or float
WEIGHT = "best_6.pt"

# Set to a filename (e.g. "rbc.mp4") to analyze only that video, or leave empty to process all in the samples folder
VIDEO_TO_ANALYZE = "fluorinated-oil.H264"  # e.g. "rbc.mp4"
